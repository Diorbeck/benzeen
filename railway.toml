[build]
installCommand = "pnpm install --no-frozen-lockfile"
buildCommand = "pnpm build && chmod +x scripts/start.sh"

[deploy]
startCommand = "sh scripts/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
